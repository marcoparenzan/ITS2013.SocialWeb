@if (ViewBag.is_authenticated)
{ 
    <text>Bentornato,</text> @ViewBag.social_state.Me.name
    <img src="@Url.Action("Picture")" />
}
else
{
    <a href="@Url.Action("Login")" class="btn btn-primary">Login con Facebook</a>
}
